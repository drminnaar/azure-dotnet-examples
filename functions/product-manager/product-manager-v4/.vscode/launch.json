{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to V4 .NET Functions",
            "type": "coreclr",
            "request": "attach",
            "processId": "${command:azureFunctions.pickProcess}"
        }
    ]
}