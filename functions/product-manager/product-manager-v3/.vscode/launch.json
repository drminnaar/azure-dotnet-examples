{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to .NET V3 Functions",
            "type": "coreclr",
            "request": "attach",
            "processId": "${command:azureFunctions.pickProcess}"
        }
    ]
}